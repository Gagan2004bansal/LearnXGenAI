1. Create a folder
2. Open it in VS code
3. Create a new virtual envirnoment (venv)
    -  python -m venv .venv
4. Activate venv
    -  source .venv/bin/activate
5. Create the requirements.txt
6. Install packages from requirements.text
    - pip install -r requirements.txt
7. Verify langchain installation
    - create test.py file 
8. Create Folder 
    - LLMs
    - ChatModels
    - EmbeddedModels
9. Take our openai api key from platform.openai.com 
10. Create .env file and store openai-api key there